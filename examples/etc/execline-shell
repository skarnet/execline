#!/bin/execlineb -S0

` -n home
{
  && -nt
  {
    ` -n user { id -un }
    $ -u user user $ -D $user USER USER
    < -w 2 /dev/null ~ ${USER}
  }
  echo /
}
$ -u home home $ -D $home HOME HOME
tryexec { ${HOME}/.execline-shell $@ }
/bin/sh $@
